name: test

on:
  push:
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v6
        with:
          go-version: '1.26.4'
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - run: go test -v -race ./cmd/...
